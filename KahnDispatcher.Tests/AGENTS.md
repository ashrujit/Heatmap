# Offline Dispatcher Verification

Link the UI sources without invoking the operator executable. Inject disposable
settings and disable sketch polling; never show a visible form or call a command handler.
Bitmap captures test layout only, not Quantower or broker connectivity.
