interface:
  display_name: "Dost"
  short_description: "Auction companion for LevelLedger and Skurry reads"
  default_prompt: "Use Dost to audit the current auction: what must be true, who has durable ownership, what failed, and what trade direction is only a probe."
