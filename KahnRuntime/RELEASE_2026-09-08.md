# Checkpoint Version Hotfix - 2026-09-08

The first Dispatch/WATCH attempt exposed a missed serialization boundary:
`RuntimeCheckpointData` defaulted to version 2, but `RuntimeCheckpointStore.Save`
overwrote it with version 1. The schema-2 transport correctly rejected this
otherwise fresh, path-correct, flat runtime checkpoint.

Both initialization and disk writes now use one `CurrentVersion` constant.
Dispatch, GO LIVE and BE safety checks are unchanged. No auction, scale, sizing,
campaign or control semantics changed; Dispatcher does not require a rebuild.

Verification: all 150 existing C# checks, two new actual disk-write cases and
24 Python transport tests passed. The cross-language regression feeds the real
C# checkpoint file into first-dispatch admission, without writing a campaign or
control. A legacy checkpoint remains rejected.

KahnRuntime Release built with zero warnings/errors and was installed at
`C:\Quantower\Settings\Scripts\Strategies\KahnRuntime\KahnRuntime.dll`.
Installed SHA256 matches the tested build:
`aac96114525b939c9c144f9f85f2410134d0679b1d833e1216225e2416066334`.
The prior binary and new build are retained locally under
`research/out/kahn-checkpoint-fix-20260908/rollback` and `release`.

Before installation ES/NQ reported fresh, Running checkpoints, zero quantity,
zero working orders and no loaded campaign. No campaign/control file or running
process was changed. Quantower caches assemblies: restart Quantower and its Kahn
instances to load the corrected writer before retrying Dispatch/WATCH. Installed
file verification does not establish that a running instance has reloaded it.

The user subsequently confirmed the retry works. This closes the reported
Dispatch/WATCH version-stamp failure, not the separate broker-lifecycle checks.
