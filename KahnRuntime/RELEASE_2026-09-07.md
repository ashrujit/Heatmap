# Kahn Schema 2 Release - 2026-09-07

The user authorized release, commit, push and scoped cleanup after the
[implementation verification](IMPLEMENTATION_COMPLETION_2026-09-07.md).

## Installed Artifacts

All projects built in Release with zero warnings and zero errors. Installed DLLs
match the previously verified workspace-local builds byte-for-byte:

| Component | Installed path | SHA256 |
| --- | --- | --- |
| KahnRuntime | `C:\Quantower\Settings\Scripts\Strategies\KahnRuntime\KahnRuntime.dll` | `c62cd3d49506194290100da1470732ac8003cb8cb607b6fac8acb4b7898bc2d0` |
| SaavikProbe | `C:\Quantower\Settings\Scripts\Indicators\SaavikProbe\SaavikProbe.dll` | `34be4f850e3a335af46c0028a3a9f796b38fa15aff30dde1cde5e8ec2ff1a58d` |
| KahnDispatcher | `C:\Heatmap\KahnDispatcher\bin\Release\net10.0-windows\KahnDispatcher.exe` and accompanying DLL | DLL: `83cdb943260b9305ad706ef88fa99df6d7da6a352b65c5cc5df011dd9a04002a` |

The Python transport/assembler and Saavik skill remain source-local at their
existing paths. The related 6J profile addition and research/session notes are
included in the source release. No unrelated project was rebuilt or deployed.

## Activation Boundary

Quantower and KahnDispatcher were not running during installation. All four
saved profile checkpoints were stopped/stale and last reported zero quantity;
that is not current broker-flat proof. ES/NQ last recorded Retired, 6J Ready;
6J/DEFAULT also had stale controls. No control was issued, acknowledged on their
behalf, deleted or migrated. Campaign/control/checkpoint files were backed up
and hash-verified unchanged after deployment.

Installation does not start strategies or authorize trades. At the next
Quantower start, verify loaded assembly provenance and actual account exposure
before starting Kahn. Old schema-1 campaign files are rejected by the new runtime;
they are not silently converted. A reviewed schema-2 dispatch loads WATCH and
needs explicit GO LIVE with unchanged entry expiry. Follow
[SCHEMA2_CUTOVER.md](SCHEMA2_CUTOVER.md) for activation and rollback precautions.

Broker-connected partial-fill, reconnect, protection/harvest, FLAT late-fill and
Probe chart-interaction checks remain pending. Release installation is not a
claim that those live acceptance checks passed.

## Verification And Retained Files

The release pass reran all 150 C# checks, 22 transport tests, 85 research tests
and Dispatcher geometry/import/layout checks successfully. The final core and
session replay manifests from implementation remain preserved without retuning.
The frozen observer retains its exact source hash across Git checkouts through
a file-scoped newline attribute, not a rewritten expected hash.

Local release archive: `research/out/kahn-release-20260907/` (git-ignored).

- `rollback/`: prior installed binaries and profile campaign/control/checkpoint
  copies. These profile copies remain local and are not part of the commit.
- `verification/`: workspace build directories, test binaries and UI screenshots
  retained before deleting redundant generated working directories. The original
  manifests retain their historical paths; this archive preserves those binaries.
- `archive-manifest.json`: generated path/SHA256 inventory of retained files.

Cleanup preserves the runnable Dispatcher Release directory, installed Quantower
artifacts, all research outputs, rollback archive, and unrelated workspace files.
