# EngineProbe

- This executable is offline research only. Link pure production source instead
  of a project reference: KahnRuntime's project output deploys into Quantower.
- Snapshot replay and policy replay are separate operations. Captured snapshots
  do not reproduce the live worker's exact sampling, L1 gates, or scheduling.
- Seeded positions are explicit counterfactuals, never reconstructed fills.
- Preserve source event order within a sample. Reordering ownership before/after
  failure changes the question under investigation.
- Never link the order gateway, runtime worker, or a broker API.
- Full-campaign interventions stay in this harness. The current-source baseline
  must retain its defects so before/after comparisons remain attributable.
- Joint observers are unapproved latest-claim experiments. Retain typed failure,
  TEST/HOLD liveness, single consumption, progression, and source risk-down priority.
  Do not imply that their price-reclaim rule is merely a memory bug fix.
- Optional formation lineage is a separate sidecar. Exporting it must leave the
  primary rail stream byte-identical; old studies must not acquire new inputs.
